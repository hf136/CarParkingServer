<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.IParking">

    <select id="getParkings" resultType="pojo.Parking">
        SELECT * FROM parking
    </select>

    <insert id="addParking" parameterType="pojo.Parking">
        INSERT INTO parking (name, latitude, longitude, address, price, amount, , available) VALUES
          (${name}, #{latitude}, #{longitude}, #{address}, #{price}, #{amount}, #{available})
    </insert>

</mapper>